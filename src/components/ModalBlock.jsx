import { Modal } from "react-bootstrap";

const ModalBlock = ({ title, visible, children}) => {
  return (
    <Modal>
      <Modal.Body>

      </Modal.Body>
    </Modal>
  );
}
 
export default ModalBlock;